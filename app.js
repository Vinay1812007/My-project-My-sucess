// app.js
// Safe JavaScript for AdSense + Vercel

document.addEventListener("DOMContentLoaded", () => {
    console.log("Website loaded successfully");

    // Example: simple greeting (optional)
    const header = document.querySelector("header p");
    if (header) {
        header.textContent = "Fast, secure & powered by Vercel";
    }
});
